# Examples

- `cleaning_config.example.yaml` — a fully-annotated config showing every rule
  and parameter. Copy it into your dataset directory as `cleaning_config.yaml`
  for auto-discovery, or pass it with `--config`.
