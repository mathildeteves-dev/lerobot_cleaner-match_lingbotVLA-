# Existing LIBERO-fastwam layout, declared once in mapping rather than quality.
canonical:
  state:
    arm:
      - {source: observation.state, start: 0, end: 6}
    gripper:
      - {source: observation.state, start: 6, end: 8}
  action:
    arm:
      - {source: action, start: 0, end: 6}
    gripper:
      - {source: action, start: 6, end: 7}
