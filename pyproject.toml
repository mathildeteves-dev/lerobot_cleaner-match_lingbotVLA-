[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "lerobot_cleaner"
version = "0.1.0"
description = "LeRobot dataset quality and cleaning with generic, GR00T and LingBot feature mappings"
readme = "README.md"
license = { text = "Apache-2.0" }
requires-python = ">=3.10"
authors = [{ name = "cyd" }]
keywords = ["lerobot", "gr00t", "robotics", "vla", "dataset", "cleaning"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: Apache Software License",
    "Operating System :: POSIX :: Linux",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
dependencies = [
    "numpy>=1.24",
    "pandas>=2.0",
    "pyarrow>=14.0",
    "pydantic>=2.5",
    "typer>=0.9",
    "rich>=13.0",
    "questionary>=2.0",
    "pyyaml>=6.0",
    "tqdm>=4.65",
]

[project.optional-dependencies]
viz = ["matplotlib>=3.7"]
video = ["opencv-python>=4.8"]
v3-video = ["av>=14", "Pillow>=9"]
dev = ["pytest>=7.4", "pytest-cov>=4.1", "ruff>=0.4"]
lerobot = [
    "lerobot @ https://github.com/huggingface/lerobot/archive/refs/tags/v0.4.2.tar.gz"
]
# Compatibility alias; the official loader is also usable without LingBot mappings.
lingbot = [
    "lerobot @ https://github.com/huggingface/lerobot/archive/refs/tags/v0.4.2.tar.gz"
]
all = [
    "matplotlib>=3.7",
    "opencv-python>=4.8",
    "pytest>=7.4",
    "ruff>=0.4",
]

[project.scripts]
lerobot_cleaner = "lerobot_cleaner.cli:app"
lerobot-cleaner = "lerobot_cleaner.cli:app"

[project.urls]
Homepage = "https://github.com/mathildeteves-dev/lerobot_cleaner-match_lingbotVLA-"
Repository = "https://github.com/mathildeteves-dev/lerobot_cleaner-match_lingbotVLA-"

[tool.setuptools.packages.find]
where = ["."]
include = ["lerobot_cleaner*"]

[tool.setuptools.package-data]
lerobot_cleaner = ["py.typed"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I"]
ignore = ["E501"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
